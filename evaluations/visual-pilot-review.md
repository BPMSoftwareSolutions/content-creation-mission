# Visual pilot review

Both v1 and director-v2 are now rejected as FEATURE_ONLY_GROUNDING under the
Mechanic-Grounded Visual Law. Earlier image-only conformance receipts do not
qualify them for publication. The new source-grounded workbench is preparation,
not an accepted replacement film.

Superseded by the Visual Experience Law. The v1 exhibit treatment has disposition
VISUAL_EXPERIENCE_DOES_NOT_CONFORM / VISUAL_PHASE_COLLAPSE. Media generation and
coverage checks passed, but that never established visual storytelling quality.
The director-v2 pass is a separate revision and requires image-bound review.

Scope: eight scenarios in resolve-governed-scenario-route. Gemini Pro generated
the storyboards; Gemini Flash TTS generated eight Kore narration chapters.
LOC_GEMINI_API_KEY was resolved without printing or saving the credential.

The first root image replaced the frozen state tablet with authorization. It
was rejected and regenerated to retain all source objects. The first fan-out
image invented a third block and removed the candidate set in its outcome.
It was rejected and regenerated with two blocks and an empty C outline retained.
Old attempts remain in raw output storage but are excluded by current job ID.

The convergence scene preserves the empty slot and ends PENDING. The bounded
return ends RETURN REJECTED without retrying. The last scene selects CONVERGENCE
PENDING. All eight scenes were inspected in a contact sheet; root, convergence
and corrected fan-out were inspected at larger size.

This is a reviewed prototype, not flawless semantic rendering. Fine print and
decorative marks vary: for example, checkbox marks on the fan-out authority
card change despite stable A/B/C labels. Replace such fine diagrams and text
with deterministic overlays before final technical publication. Exact source
meaning remains accessible in the atlas.

Browser checks exercised pilot filtering, next navigation and phase playback.
Content controls download generation requests rather than finished social assets.
The 3m51s walkthrough uses Gemini narration and phase dissolves. The vertical
60-second cut is silent. Neither is a simulation, runtime trace or generative
motion. The remaining 815 scenarios have specs, not images. This proof does not
close whole-estate classification or visual quality.

Speech request format follows Google's
[speech generation documentation](https://ai.google.dev/gemini-api/docs/generate-content/speech-generation).
Audio is mono 24kHz PCM WAV before AAC video export.
